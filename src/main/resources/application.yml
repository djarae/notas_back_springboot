spring:
  application:
    name: notas
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://ep-noisy-wind-auvjlq6g-pooler.c-10.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
    username: ${DATABASE_USERNAME:neondb_owner}
    password: ${DATABASE_PASSWORD:npg_ewKR8QPNCD4Y}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

app:
  jwt:
    secret: ${JWT_SECRET:UnSecretoMuyLargoParaElJWTDeKomodoNotasAppQueDebeSerMayorA256BitsParaSerSeguro}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000} # 24 horas
  resend:
    api-key: ${RESEND_API_KEY:re_VNzZtd3i_8DLwFpsMESwbT1T8L3jBhtnc}
    from-email: ${RESEND_FROM_EMAIL:onboarding@resend.dev}
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:4200}

